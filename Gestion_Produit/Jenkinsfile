pipeline {
  agent any

  tools {
    maven 'maven-3'
  }

  environment {
    APP_DIR   = "Gestion_Produit"
    IMAGE_TAG = "v1.${BUILD_NUMBER}"
    IMAGE     = "harbor.local:8081/staging/gestion:${IMAGE_TAG}"
  }

  stages {

    stage('Lint') {
      steps {
        dir("${APP_DIR}") {
          sh '''
            echo "verification regles de style"
            mvn checkstyle:check -Dcheckstyle.failOnViolation=false
          '''
        }
        sh '''
          echo "verification dockerfile"
          hadolint ${APP_DIR}/Dockerfile || true

          echo "verification yaml"
          yamllint . || true
        '''
      }
    }

    stage("Build"){
      steps{
        dir("${APP_DIR}") {
          sh '''
            echo "production de l'artefact(.jar)"
            mvn clean package 

         '''
       }
      }
    }
    stage('Tests') {
      steps {
        dir("${APP_DIR}") {
          sh '''
            echo "tests unitaires"
            mvn test
          '''
        }
      }
      post {
       always {
        junit "${APP_DIR}/target/surefire-reports/*.xml"
       }
     }
    }

    stage('SonarQube Analysis') {
      steps {
        dir("${APP_DIR}") {
          withSonarQubeEnv('sonarqube') {
            sh '''
              mvn org.sonarsource.scanner.maven:sonar-maven-plugin:3.10.0.2594:sonar \
                -Dsonar.projectKey=my-gestion-app \
                -Dsonar.projectName=my-gestion-app \
                -Dsonar.java.binaries=target
            '''
          }
        }
      }
    }

    stage('Dependency Check (SCA)') {
      steps {
        dir("${APP_DIR}") {
          sh 'mvn verify'
        }
      }
      post {
        always {
          archiveArtifacts artifacts: 'Gestion_Produit/target/dependency-check-report.html'
        }
      }
    }

    stage('Build Image') {
      steps {
        dir("${APP_DIR}") {
          sh 'docker build -t $IMAGE .'
        }
      }
    }

    stage('Scan Image (Trivy)') {
      steps {
        sh '''
          trivy image \
            --severity HIGH,CRITICAL \
            --ignore-unfixed \
            --exit-code 1 \
            $IMAGE
        '''
      }
    }

    stage('Generate SBOM (Syft)') {
      steps {
        sh 'syft $IMAGE -o json > sbom.json'
        archiveArtifacts artifacts: 'sbom.json'
      }
    }

    stage('Sign Image (Cosign)') {
      environment {
        COSIGN_PASSWORD = credentials('cosign_password')
      }
      steps {
        withCredentials([
          file(credentialsId: 'cosign-private-key', variable: 'COSIGN_KEY')
        ]) {
          sh '''
            cosign sign \
              --key $COSIGN_KEY \
              $IMAGE
          '''
        }
      }
    }

    stage('Push Image to Harbor') {
      steps {
        sh 'docker push $IMAGE'
      }
    }
  }
}
